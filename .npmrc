# Expo + React Native peer dependencies across a workspace frequently disagree on
# exact versions; allow installs to proceed rather than hard-failing on peer ranges.
legacy-peer-deps=true
engine-strict=false
